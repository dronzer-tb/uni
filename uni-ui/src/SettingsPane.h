#pragma once

#include <QWidget>

// Placeholder for M5. Provides backend toggles + benchmark viewer.
class SettingsPane : public QWidget {
    Q_OBJECT
public:
    explicit SettingsPane(QWidget *parent = nullptr);
};
